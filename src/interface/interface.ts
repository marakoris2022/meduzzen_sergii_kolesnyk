export enum PATHS {
  MAIN = "/",
  PROFILE = "/profile",
  USERS = "/users",
  COMPANIES = "/companies",
  ABOUT = "/about",
  SIGNIN = "/signin",
  SIGNUP = "/signup",
}

export enum TOKEN {
  NAME = "authToken",
}
